<div id="<%= dom_id meeting %>">
  <p>
    <strong>Evento:</strong>
    <%= t("activerecord.attributes.meeting.names.#{meeting.name}").humanize %>
  </p>

  <p>
    <strong>Fecha:</strong>
    <%= meeting.start_time %>
  </p>

</div>
